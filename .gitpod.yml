
tasks:
  - init: |
      docker-compose -f local.yml pull
  - command: |
      docker-compose -f local.yml up
ports:
  - port: 8000
    onOpen: open-browser
    visibility: public

vscode:
  extensions:
    - ms-azuretools.vscode-docker